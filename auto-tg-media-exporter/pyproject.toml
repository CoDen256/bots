[project]
name = "auto-tg-media-exporter"
version = "0.1.0"
description = "Script to export media from telegram"
requires-python = ">=3.14"
dependencies = [
    "telethon",
    "core-bots",
    "rich",
    "hachoir",
    "cryptg"
]
