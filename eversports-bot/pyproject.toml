[project]
name = "eversports-bot"
version = "0.1.0"
description = "Finds a spot to book a court for via eversports"
requires-python = ">=3.14"
dependencies = [
    "core-bots",
    "configparser",
    "yaml"
]
