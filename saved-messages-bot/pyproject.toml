[project]
name = "saved-messages-bot"
version = "0.1.0"
description = "Saved Messages Manager Telegram Client"
requires-python = ">=3.14"
dependencies = [
    "telethon",
    "core-bots"
]

[tool.uv]
exclude-dependencies = ["pyTelegramBotAPI"]